# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is DELIBERATELY NOT ignored: serve.go embeds webui/dist via Go's
# embed directive, so the built bundle must be committed for a fresh
# checkout to `go build` at all — see CLAUDE.md's Build/test section.
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
